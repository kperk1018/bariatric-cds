# PHI — never commit the CSV
data/*
!data/.gitkeep

# Ioanna's 1A materials — ignored until reviewed for PHI, then selectively un-ignore clean code
reference/ioanna/*
!reference/ioanna/README.md

# Artifacts: model weights are safe to commit (no PHI); exclude only raw data files
artifacts/*
!artifacts/.gitkeep
!artifacts/*.joblib
!artifacts/*.png

__pycache__/
*.pyc
.venv/
.DS_Store
