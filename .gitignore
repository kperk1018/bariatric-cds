# PHI — never commit the CSV
data/*
!data/.gitkeep

# Ioanna's 1A materials + our alignment reports — NEVER commit. References her
# unpublished manuscript and her dataset's flaws; repo is public.
reference/

# Artifacts: model weights are safe to commit (no PHI); exclude only raw data files
artifacts/*
!artifacts/.gitkeep
!artifacts/*.joblib
!artifacts/*.png

__pycache__/
*.pyc
.venv/
.DS_Store
